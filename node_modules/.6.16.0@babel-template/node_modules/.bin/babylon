#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../.6.11.6@babylon/bin/babylon.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../.6.11.6@babylon/bin/babylon.js" "$@"
  ret=$?
fi
exit $ret
