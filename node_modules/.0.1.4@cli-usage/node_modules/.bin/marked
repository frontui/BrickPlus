#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../.0.3.6@marked/bin/marked" "$@"
  ret=$?
else 
  node  "$basedir/../../../.0.3.6@marked/bin/marked" "$@"
  ret=$?
fi
exit $ret
